name: build
on: [push]
jobs:
  curl:
    runs-on: ubuntu-latest
    steps:
      - name: lhci-pokedex
        run: |
          curl https://webhook.mattycakes.ca/hooks/lhci-pokedex-documentation?token=${{ secrets.LHCI_WEBHOOK }}
